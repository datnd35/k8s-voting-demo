# voting-server

Express service that accepts votes and stores counts in Redis.

## Run locally

Environment variables:
- REDIS_HOST (default: localhost)
- REDIS_PORT (default: 6379)

Start:
npm install
npm start

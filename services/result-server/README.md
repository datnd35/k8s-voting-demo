# result-server

Express service that reads vote totals from Redis and displays results.

## Run locally

Environment variables:
- REDIS_HOST (default: localhost)
- REDIS_PORT (default: 6379)

Start:
npm install
npm start
